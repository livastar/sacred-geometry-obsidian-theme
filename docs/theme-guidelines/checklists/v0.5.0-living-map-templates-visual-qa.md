# 0.5.0 Living Map Templates Visual QA

Use this release evidence for the `0.5.0` Living Map Templates PR.

## Static Checks

- [x] Every new `.canvas` file is valid JSON.
- [x] Every new `.canvas` file uses Obsidian Canvas `type: "canvas"` and `version: "0.2.1"`.
- [x] Every template keeps node text short enough for overview-scale scanning.
- [x] Every template uses the existing role language: mission, core-system, agent, resource, interaction, knowledge, decision, outcome.
- [x] No private vault paths, client names, personal notes, remote assets, or non-portable links are included.
- [x] No template claims SacraMap behavior, automated layout, infinite zoom, live playback, system health computation, plugin panels, or commands.
- [x] No CSS or public token changes were required for this template-only release.

## Template Coverage

- [x] `canvas/templates/Agent Ecosystem.canvas`
- [x] `canvas/templates/Project Constellation.canvas`
- [x] `canvas/templates/Decision Tree.canvas`
- [x] `canvas/templates/Knowledge Atlas.canvas`
- [x] `canvas/templates/Flow Network.canvas`
- [x] `canvas/templates/Resource Map.canvas`

## Manual Obsidian Smoke

- [x] Open each template in a clean Obsidian vault before final public announcement.
- [x] Verify dark mode readability.
- [x] Verify light mode readability.
- [x] Enable `snippets/SG Canvas Cards.css` and verify optional role styling remains readable.

## Manual Test Session

- Date: 2026-05-16.
- App: Obsidian desktop on macOS.
- Vault: clean temporary vault at `/tmp/sgs-obsidian-theme-test-vault`.
- Theme install: copied `manifest.json`, `theme.css`, and `versions.json` into `.obsidian/themes/Sacred Geometry Systems/`.
- Snippet install: copied and enabled `snippets/SG Canvas Cards.css`.
- Files opened in Obsidian Canvas:
  - `canvas/templates/Agent Ecosystem.canvas`
  - `canvas/templates/Project Constellation.canvas`
  - `canvas/templates/Decision Tree.canvas`
  - `canvas/templates/Knowledge Atlas.canvas`
  - `canvas/templates/Flow Network.canvas`
  - `canvas/templates/Resource Map.canvas`
  - `canvas/Sacred Atlas Starter.canvas`
- Result: pass. The canvases opened without blank states or missing-node errors in both dark and light mode. Role color borders, node text, arrows, canvas grid, tab chrome, file tree, and Canvas controls remained readable with the optional Canvas card snippet enabled.

The scheduled release gate uses the static checks above as repository evidence. The manual Obsidian smoke guard is complete for the `0.5.0` PR.
